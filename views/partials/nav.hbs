<nav class="absolute z-50 top-0 left-0 right-0
{{#if isIndex}} bg-transparent 
{{else}} bg-black shadow-xl
{{/if}}
">
    <div class="container mx-auto px-4">
        <div class="relative flex items-center justify-between h-16">
            <div class="flex-1 flex items-center justify-between">
                <div class="flex-shrink-0 flex items-center">

                    <a href="/" class="text-gray-100 hover:bg-blue-200 hover:bg-opacity-10 hover:text-blue-500 transition-all px-4 py-2 rounded-xl text-sm
                font-bold">
                        🐒 Ape Tavern
                    </a>

                </div>

                <!-- This stuff hides on mobile, works for now but we need a proper menu later -->
                <div class="hidden space-x-4 sm:flex">
                    <a href="/team"
                        class="text-gray-100 hover:bg-blue-200 hover:bg-opacity-10 hover:text-blue-500 transition-all px-4 py-2 rounded-xl text-sm">
                        <i class="fas fa-users"></i>
                        Team
                    </a>
                </div>

            </div>
        </div>
    </div>
</nav>